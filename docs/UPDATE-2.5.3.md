# MathCat Lab 2.5.3 源码更新

## 本版变化

- 白板按问题、实验室、证明树与提问箱、人工审核、成果和其他记录排列。
- 猫猫任务采用紧凑展开列表，实验室不展示审核猫；详情仅保留完成的文本回复，底层执行记录保留。
- 改进证明目录及依赖图阅读，补齐公式、复杂上下标、Markdown 表格和开放疑点字段渲染。
- 阶段报告和论文整理入口同行，保留历史交付、资料和目录入口。
- 修复新一轮弹窗背景、按钮和宽度；启动未指定模式时与前端统一默认自动安排，显式选择与重试仍保留。
- 增加版本检查和保留本地数据的更新入口。
- 后端从运行程序位置识别安装目录，修复打包后移动目录无法启动的问题，仍检查写入边界及链接逃逸。

## 验证（2026-09-10）

- 干净源码目录 Node 平台测试：374/374 通过。
- 证明图和目录、更新及删除保护、跨平台启动工具测试通过。
- 独立实例真实 HTTP 检查通过，覆盖版本、更新入口、来源限制和无新研究调用。
- 后端启动程序的 5 项路径测试通过，包括移动安装目录和链接逃逸。
- Rust workspace 回归在 research-core 停止：该模块 215 项通过、2 项失败、1 项忽略；此前运行的模块共57项通过。两项失败在单独复测中仍于等待预期状态时超时：
  - research_v2::tests::stopping_unknown_delivery_cannot_claim_completion_or_reopen_budget
  - research_v2::tests::unknown_delivery_cannot_resume_or_reclaim_budget
- 以上两项所在 research-core 源码与上版一致，尚未解决，不宣称完整 Rust 回归或发布验收通过。
- 本次没有真实模型调用、数学正确性验收或真实 macOS 验收。此提交是源码更新，不是新安装包发布。

## 数据保护

源码不包含本地对话、数据库、研究工作区、认证信息或机器更新指针。更新前仍应保留本地数据备份。已有版本的历史研究文件不随此次 Git 提交修改。
